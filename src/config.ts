/**
 * Application configuration
 */

export const config = {
  // Backend API URL - update this to your actual backend URL
  BACKEND_URL: import.meta.env.VITE_BACKEND_URL || 'https://api.example.com'
}
